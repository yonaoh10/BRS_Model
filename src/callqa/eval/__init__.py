"""System evaluation against a golden reference set.

Reads/produces core artifacts and compares them to references; like ops/, it
imports the core but never dashboard/, and the core never imports it.
"""
