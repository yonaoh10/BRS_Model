# Bank-server engine dependencies (GPU / model-serving machine only).
# Installed on top of requirements.txt by scripts/install_offline.sh --server.
# All of these are imported lazily; the callqa package imports cleanly without them.
faster-whisper==1.1.1
ctranslate2==4.5.0
presidio-analyzer==2.2.357
presidio-anonymizer==2.2.357
pyannote.audio==3.3.2
torch==2.5.1
torchaudio==2.5.1
transformers==4.48.0
# vLLM is typically installed separately (its own CUDA-matched wheel):
# vllm==0.6.6
