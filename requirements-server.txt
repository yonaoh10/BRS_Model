# Bank-server engine dependencies (GPU / model-serving machine only).
# Installed on top of requirements.txt by `scripts/install.py --server`.
# All of these are imported lazily; the callqa package imports cleanly without them.
faster-whisper==1.1.1
silero-vad==6.2.1
ctranslate2==4.5.0
presidio-analyzer==2.2.357
presidio-anonymizer==2.2.357
# 4.x is required for the community-1 pipeline and its exclusive output.
pyannote.audio==4.0.7
# pyannote.audio 4.x requires torch/torchaudio >= 2.8 and torchcodec, which
# torchaudio now needs for any audio I/O it still performs.
torch==2.8.0
torchaudio==2.8.0
torchcodec==0.7.0
transformers==4.48.0
# vLLM is typically installed separately (its own CUDA-matched wheel):
# vllm==0.11.2
#
# WARNING: installing vLLM after these pins can UPGRADE torch (vllm 0.11.2
# brings torch 2.9). torchcodec is ABI-paired with torch and does not fail
# with an ImportError when mismatched - it core-dumps the interpreter
# (std::length_error) the moment pyannote imports it. If vLLM moved torch,
# reinstall the matching pair, e.g. torch 2.9.x -> torchcodec==0.8.*.
