__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/
wheels/
data/
models/
*.db
*.sqlite
.venv/
venv/
MODELS_MANIFEST.json
dashboard/qa-output/

# Recordings and anything derived from them never belong in version control.
*.wav
*.mp3
*.m4a
*.aac
*.flac
*.ogg
*.opus
refs/
logs/
.env
wheels/

# Data backups (contain raw, un-redacted transcripts + the state DB) must never
# be committed. Belt-and-suspenders alongside the data/ rule above.
*.tar.gz
*.tgz
*.zip
data_backup*

# OS cruft
.DS_Store

# scratch / QA working files
scratchpad/

# Internal working notes for the AI assistant; not part of the deliverable.
HANDOVER.md

# Third-party programs a user unzips next to the project (ffmpeg, llama.cpp)
# and quantized models: hundreds of MB that never belong in version control.
tools/
*.gguf
