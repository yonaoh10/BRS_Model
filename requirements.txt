# Core runtime dependencies (mock mode runs with these alone).
# Heavy model engines are pinned in requirements-server.txt and are only
# needed on the bank server; every real engine is behind a lazy import.
#
# pytest and ruff are here on purpose: the offline bundle built from this file
# has to be able to run the test suite on the air-gapped machine, which is the
# only way to prove the install there.
#
# pydantic-settings was pinned here and imported nowhere - a dependency the
# bank would have had to review, vendor and keep patched for nothing.
pydantic==2.13.4
Jinja2==3.1.6
# 6.0.3, not 6.0.1: pyannote.audio (via pyannote-database) needs >=6.0.2, and a
# pin the engines cannot share made pip replace it behind this file's back.
PyYAML==6.0.3
numpy==2.4.6
scikit-learn==1.9.0
pytest==9.1.1
ruff==0.16.4
